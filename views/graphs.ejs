<!DOCTYPE html>
<html lang="hr">
  <head>
    <meta charset="UTF-8" />
    <title>Statistika učenja</title>
    <link rel="stylesheet" href="/styles/app.css" />
  </head>
  <body>
    <header class="topbar">
      <a class="brand" href="/app">LearnFlow</a>

      <nav class="nav">
        <a href="/subjects">Predmeti</a>
        <a href="/study-session">Sesije</a>
        <a class="active" href="/graph">Grafovi</a>

        <% if (user) { %>
        <span class="userpill"><%= user.username %></span>
        <a class="muted" href="/user/logout">Odjava</a>
        <% } else { %>
        <a href="/user/login">Login</a>
        <% } %>
      </nav>
    </header>
    <main class="container">
        <div class="page-head">
          <div>
            <h1>Statistika učenja</h1>
            <p class="subtitle">Analiziraj svoj napredak kroz grafove 📈</p>
          </div>
            <select id="timeframe">
            <option value="day">Dan</option>
            <option value="week">Tjedan</option>
            <option value="month">Mjesec</option>
            </select>
        </div>

        <div id="graphContainer" data-user-id="<%= user ? user.id : '' %>">
            <div id="graph1">
            <div class="plot"></div>
            </div>

            <div id="graph2">
            <div class="plot"></div>
            </div>

            <div id="graph3">
            <div class="plot"></div>
            </div>

            <div id="graph4">
            <div class="plot"></div>
            </div>

           <div id="graph5-card" class="card heatmap-card">
            <h3 class="heatmap-title">Dnevna aktivnost učenja</h3>
            <p class="heatmap-desc">Tvoja aktivnost zadnjih 50 dana (tamnija boja = više minuta učenja po danu)</p>
            <div id="graph5"></div>
           </div>

        </div>

        <script src="https://cdn.plot.ly/plotly-2.30.0.min.js"></script>
        <script src="/scripts/graphs.js"></script>
    </main>
  </body>
</html>
